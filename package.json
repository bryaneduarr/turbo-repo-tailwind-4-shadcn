{
  "name": "turbostack",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "clean": "turbo clean && rm -rf .turbo/ node_modules",
    "check-types": "turbo check-types",
    "dev": "turbo dev",
    "format": "biome format --write . && turbo format",
    "lint": "biome lint . && turbo lint",
    "update": "turbo update",
    "setup": "bun run clean && rm -rf bun-lock.yaml && bun install && bun run format && bun run lint && bun run check-types && bun run build && bun run dev",
    "setup:update": "bun run clean && rm -rf bun-lock.yaml && bun install && bun run update && bun run clean && rm -rf bun-lock.yaml && bun install && bun run format && bun run lint && bun run check-types && bun run build && bun run dev",
    "start": "turbo start"
  },
  "workspaces": [
    "apps/*",
    "config/*",
    "packages/*"
  ],
  "devDependencies": {
    "@workspace/biome": "workspace:*",
    "@workspace/typescript": "workspace:*",
    "turbo": "^2.6.3"
  },
  "packageManager": "bun@1.3.4",
  "engines": {
    "node": ">=20"
  }
}
